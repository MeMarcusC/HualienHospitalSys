{
    "version": "0.2.0",
    "configurations": [
       
        {
            "name": "C++ Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/MarcusHospitalSys",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
        }
    ]
}